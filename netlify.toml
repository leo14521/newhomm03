# Next.js — Netlify가 저장소 연결 시 OpenNext 어댑터를 자동 적용합니다.
# package.json에 @netlify/plugin-nextjs를 고정하면 플랫폼과 버전 충돌이 날 수 있어 제외했습니다.
# https://docs.netlify.com/frameworks/next-js/overview/

[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"
